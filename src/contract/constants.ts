export const EMPTY_PDA_RENT_EXEMPT_BALANCE = 890880;

// Byte array size constants (must match Rust constants)
export const PASSKEY_PUBLIC_KEY_SIZE = 33;
export const CREDENTIAL_HASH_SIZE = 32;
export const SIGNATURE_SIZE = 64;
